# 🚀 Sonic VPN: запускаем с нуля (объясняю на пальцах)

> Этот документ для человека, который НИКОГДА не работал с серверами.
> Каждый шаг: **что это → что нажать → что вписать → как проверить**.
> Время: 2–4 часа работы + ожидание одобрения самозанятости (параллельно).
> Деньги: ~400–900 ₽/мес, все оплаты — картой РФ (МИР) или СБП.

---

## Часть 1. Что мы вообще строим (на пальцах)

У нас готовый интернет-сервис из 5 кусков. Вот что такое каждый кусок:

| Кусок | Это | Почему нужен |
|---|---|---|
| **VPS в Германии** | Арендованный компьютер в чужом дата-центре, работает 24/7, ~400–600 ₽/мес. Покупаем у **российского** продавца (AdminVPS/Timeweb), но **сервер стоит в Германии** | На нём живёт наш VPN-сервер. Сервер обязан быть за рубежом (иначе не работать), а продаём мы из РФ |
| **VPN-софт (3x-ui)** | Бесплатная программа на VPS, которая выдаёт людям «ключи» для подключения VPN | Это и есть сам VPN. У каждого клиента свой ключ |
| **Сайт sonicvpn.ru** | Наш лендинг + личный кабинет. Обычная веб-страница, тоже живёт на том же VPS | Люди покупают VPN здесь, получают QR-код для подключения |
| **Домен** | Адрес `sonicvpn.ru` (как «улица и номер дома»), ~250 ₽/год | Чтобы сайт открывался по красивому адресу, а не по цифрам IP |
| **Cloudflare** | Бесплатная «защита-курьер» между посетителем и нашим VPS | (1) сайт открывается быстро и в РФ, даже если что-то блокируют; (2) реальный адрес VPS прячется от блокировщиков |
| **Telegram-бот** | Бесплатный чат-бот (Telegram ничего не стоит) | Люди покупают VPN, получают QR, спрашивают поддержку — прямо в Telegram |
| **Нейросеть поддержки** | «Мозг» бота. Арендуем бесплатно у компании Groq | Бот отвечает на вопросы клиентов как живой консультант |
| **ЮKassa** | Платёжный терминал (как эквайринг): принимает **СБП** от клиентов и перечисляет деньги вам (нужен статус самозанятого) | Чтобы люди платили QR-кодом из приложения банка, а деньги падали на ваш счёт |
| **.env файл** | Текстовый файл на VPS со всеми «секретными ключами» (от платежей, бота, нейросети) | Программы читают его и понимают, куда подключаться |
| **pm2** | Маленькая программа-«староста»: если сайт упадёт — сам его поднимет | Чтобы ночью не просыпаться |

**Схема одним взглядом:**

```
Покупатель (РФ, телефон)
   │  1. открывает sonicvpn.ru  ←→  Cloudflare (бесплатно, прячет VPS)
   │  2. платит СБП-QR           ←→  ЮKassa → деньги ВАМ
   │  3. сканирует QR-профиль    ←→  VPN-сервер 3x-ui на VPS (Германия)
   │  4. пишет в бота            ←→  нейросеть Groq (бесплатно)
   ▼
Ваш VPS в Германии (400–600 ₽/мес) — всё, что платите
```

## Словарь (если слово непонятно — перелистни сюда)

- **IP-адрес** — «телефонный номер» компьютера в интернете, выглядит как `185.14.23.107`.
- **SSH** — способ «сесть внутрь» удалённого компьютера через текст: вы печатаете команды, как в обычном терминале. На Windows 10/11 есть встроено (программа PowerShell).
- **Терминал** — чёрное окно, куда печатают команды. Windows: нажать `Win` → написать `powershell` → Enter.
- **Команда** — одна строчка текста в терминале (например `cd /opt/sonicvpn`). В этом документе команды в рамочках — копируйте их целиком.
- **nano** — простой текстовый редактор прямо в терминале (в нём редактируем .env). Управление: стрелки — движение, `Ctrl+O` → `Enter` — сохранить, `Ctrl+X` — выйти.
- **Токен / API-ключ** — длинная строка-пароль, по которой наш код узнаётся у чужого сервиса (Telegram, Groq, ЮKassa). Хранится только в .env, никому не показывать.
- **Webhook** — «звонок»: когда клиент оплатил в ЮKassa, она сама «звонит» нашему сайту, и тот активирует VPN. Настраивается один раз.
- **NS-записи (nameservers)** — два адреса вида `xxx.ns.cloudflare.com`, которые говорят интернету «домен sonicvpn.ru обслуживает Cloudflare». В Cloudflare их увидим, в reg.ru впишем.
- **Прокси (в Cloudflare)** — «оранжевое облако» у DNS-записи: включает защиту/CDN. У наших A-записей — ВКЛ.

---

## Часть 2. Шаги

### Шаг 0. Самозанятость (сегодня, 15 минут, телефон)

**Зачем:** без статуса самозанятого ЮKassa не примет деньги от клиентов. Одобрение идёт до 5 рабочих дней — поэтому стартуем СЕГОДНЯ, пока разворачиваем остальное.

1. Телефон: приложение **«Мой налог»** (App Store / Google Play / RuStore).
2. «Зарегистрироваться» → тип: **самозанятый**.
3. Приложение попросит: ФИО, паспорт, СНИЛС (если используете Госуслуги — подтянется само).
4. В конце: «Вы стали самозанятым» — **сфотографируйте/сохраните** подтверждение (придёт в кабинете приложения).
5. Открыть **yookassa.ru** → «Подключиться» → выберите «Самозанятые» → введите свои данные (ИНН) → отправьте заявку.
6. Ждём 1–5 рабочих дней (напишут на почту). **Пока ждём — работаем дальше, всё будет в тестовом режиме.**

✅ **Проверка:** в «Мое налоге» статус «действует»; на почте — письмо от ЮKassa (хотя бы «заявка принята»).

---

### Шаг 1. Покупаем VPS (30 минут, компьютер, ~300–600 ₽/мес)

**Зачем:** это «арендованный компьютер» в Германии, на котором будет жить весь сервис.

1. Открыть сайт **adminvps.ru** (если вдруг не работает — альтернатива: **timeweb.cloud** — там всё по аналогии).
2. Зарегистрироваться (кнопка «Регистрация» сверху): phone (ваш), email, пароль.
3. Меню: **VPS → Германия** (локация DE).
4. Выбрать тариф по параметрам: **2 vCPU, 4 GB RAM, 40–60 GB NVMe-диска**. (Не берите 1 GB — мало. Не берите 16 GB — дорого и не нужно.)
5. Операционная система: **Ubuntu 24.04** (или 22.04). Важно: именно Ubuntu.
6. Имя сервера: любое (например `sonic1`).
7. **Оплата: МИР-карта или СБП** — это и есть «оплата из России», работает штатно.
8. **Очень важно:** в личном кабинете (или при оплате) включите **автоплатёж** (авторасширение) по вашей МИР-карте — иначе через месяц сервер отключат, и VPN у клиентов слетит.
9. Ждём 5–10 минут. В кабинете появится сервер со статусом «работает» и данными:
   - **IP-адрес** (например `185.14.23.107`)
   - **Пароль root** (иногда — кнопка «Сбросить пароль», чтобы увидеть новый)

📝 **Запишите в заметки (будут нужны в шагах 2, 4, 5):**
```
IP VPS:      185.14.23.107        ← ВАШ адрес
Пароль root: ********
```

> 💡 **Если есть зарубежная банковская карта** (не наша): можно взять Oracle Cloud Always Free — тот же VPS, но 0 ₽ (4 ядра, 24 GB, бесплатно навсегда). Дальнейшие шаги идентичны. Если зарубежной карты нет — AdminVPS, не думайте об этом.

✅ **Проверка:** в кабинете провайдера сервер «в сети», IP отображается.

---

### Шаг 2. Учимся «входить» на VPS (5 минут)

**Зачем:** дальше все команды (установка программ, запуск сайта) мы будем печатать «внутри» VPS через SSH.

**На Windows 10/11:**
1. Нажмите клавишу **Win** (с логотипом Windows) → начните печатать `powershell` → нажмите Enter. Откроется синее окно — это терминал.
2. Впишите (вместо `ВАШ_IP` — адрес из шага 1) и нажмите Enter:
   ```
   ssh root@185.14.23.107
   ```
3. Первый раз спросит `Are you sure you want to continue connecting?` → впишите `yes` → Enter.
4. `Password:` → впишите пароль root **НЕВИДИМО** (ничего не светится, это нормально) → Enter.
5. Появилась строка вида `root@sonic1:~#` — **вы внутри VPS**. Отлично.

**На Mac/iPhone (терминал):** та же команда `ssh root@185.14.23.107`.

> Выйти из VPS: вписать `exit`.
> Не работает / «Connection refused» → перезагрузите сервер в кабинете провайдера, попробуйте через минуту.

✅ **Проверка:** вы видите приглашение `root@...:~#` — это значит, что SSH работает.

---

### Шаг 3. Ставим всё необходимое одним скриптом (15 минут)

**Зачем:** на VPS нужно установить VPN-софт (3x-ui), сайт-обвязку (Caddy — выдаёт бесплатные SSL-сертификаты «HTTPS»), защиту и Node.js (язык, на котором написан наш сервис). Скрипт `deploy/vps-setup.sh` из нашего репозитория делает это всё за вас.

**Получаем скрипт:**
1. На компьютере открыть: **https://github.com/samagon90/vpnStar/blob/arena/01a05219-vpnstar/deploy/vps-setup.sh**
2. На странице нажмите кнопку **«Copy raw file»** (справа сверху, иконка скопирования) — весь текст скрипта скопирован в буфер.

**Кладём его на VPS (вы уже в ssh, см. шаг 2):**
1. Впишите: `nano /root/vps-setup.sh` → Enter (откроется редактор, файл пустой).
2. Вставьте скопированное: на Windows — **правая кнопка мыши** в окне PowerShell (или `Ctrl+Shift+V`).
3. `Ctrl+O` → Enter (сохранить) → `Ctrl+X` (выйти).
4. Запускаем (подставив свой будущий домен):
   ```
   bash /root/vps-setup.sh sonicvpn.ru
   ```
5. Ждём **5–15 минут**. Будут лететь строки установки. Скрипт ставит: 3x-ui, Caddy, UFW (firewall), fail2ban, Node.js 22, pm2, swap.

В конце скрипт напишет блок «ГОТОВО» и покажет, где панель 3x-ui. **Пароль/URL панели 3x-ui** выдаст сам установщик 3x-ui (строка вида «X-UI installation was successful, Panel: http://ВАШ_IP:порт, username: ..., password: ...») — она проматнется в потоке строк. Если не успели поймать — вынесите панель ещё раз:
```
x-ui settings   # или в логах: journalctl -u 3x-ui -n 100
```
(В современных версиях 3x-ui панель открывается по адресу, который напечатал установщик; логин/пароль — там же. Если что-то из них утеряно: `x-ui reset` сменит пароль и напечатает новый.)

📝 **Запишите (шаг 4 и .env):**
```
Панель 3x-ui:  http://185.14.23.107:2053   (URL с портом)
Логин панели:  xxx
Пароль панели: yyy
```

> ⚠️ Сразу после первого входа в панель 3x-ui **смените логин/пароль** (Settings → Modify password) и запишите новые.

✅ **Проверка:** в браузере (компьютера) откройте `http://185.14.23.107:2053` (ваш URL) → открылась страница входа 3x-ui.

---

### Шаг 4. Настраиваем VPN в 3x-ui (20 минут)

**Зачем:** 3x-ui установлен, но «ключи» для подключения людей ещё не созданы. Делаем два сервера подключения: основной (VLESS+Reality — не блокируется) и запасной (Hysteria2 — быстрый).

В панели 3x-ui (вы в ней по адресу из шага 3):

1. **Основной сервер:**
   - Меню слева: **Inbounds → Add Inbound** (кнопка сверху).
   - **Protocol:** `vless`
   - **Port:** `443`
   - **Security:** `reality`
   - Внизу откроется блок **Reality Settings**:
     - Рядом с **Private Key** нажмите кнопку **Generate** (сгенерировать).
     - Соседнее поле **Public Key** заполнится само — **скопируйте этот Public Key** (он понадобится в .env, шаг 6).
     - **SNI:** впишите `www.microsoft.com`
     - **Short ID:** можно пустым
     - **Spider Path:** `/`
   - В блоке **Settings → Clients** нажмите **Add Client** — появится клиент с авто-сгенерированным ключом (id/email). Это «образец» — по нему вы протестируете.
   - **Save**.

2. **Запасной сервер:**
   - **Inbounds → Add Inbound**
   - **Protocol:** `hysteria2`
   - **Port:** `8443`
   - **Security:** `none`
   - **Settings → Clients → Add Client**
   - **Save**.

3. **Тест из России (с телефона):**
   - Телефон: установите **v2rayNG** (Android, Play Store) или **Streisand** (iOS).
   - В панели 3x-ui: Inbounds → ваш vless → **Settings → Clients → у первого клиента — иконка QR** → отсканируйте камерой телефона прямо в v2rayNG («Импорт из камеры»).
   - В v2rayNG нажмите на профиль → **подключить**.
   - Откройте YouTube. **Работает — узел живой.** 🎉

📝 **Запишите (в .env, шаг 6):**
```
Public Key (Reality):  PBkAbC...   ← из шага 4.1
```

✅ **Проверка:** YouTube/Google открываются на телефоне через VPN.

---

### Шаг 5. Домен + Cloudflare (20 минут, ~250 ₽/год)

**Зачем:** красивый адрес сайта + защита от блокировок + быстрая работа в РФ.

**5.1. Покупаем домен:**
1. **reg.ru** → в поиске доменов введите `sonicvpn.ru` → если «свободен» → в корзину → оплата **СБП/картой РФ** (~250–350 ₽/год).
2. Заодно возьмите запасной: `sonic-vpn.com` (~350 ₽/год). Зачем: если .ru когда-нибудь заденут — переключимся за 5 минут.

**5.2. Подключаем Cloudflare:**
1. **dash.cloudflare.com** → **Sign up** (бесплатно, email + пароль).
2. **Add a site** → впишите `sonicvpn.ru` → план **Free** → Continue.
3. Cloudflare покажет **NS-записи** — две строки вида:
   ```
   anna.ns.cloudflare.com
   bob.ns.cloudflare.com
   ```
   (имена будут другие — свои). **Скопируйте обе.**
4. Вернитесь в **reg.ru** → ваш домен → **Управление доменом → Nameservers** → «Изменить» → впишите две строки из Cloudflare → сохранить.
5. Ждём, пока Cloudflare напишет на почту «Active» (от 10 минут до пары часов; можно проверить статус на странице домена в Cloudflare).

**5.3. DNS-записи в Cloudflare** (страница **DNS → Records**):
Нажмите «Add record» и создайте **две** записи:

| Type | Name | Content | Proxy status |
|---|---|---|---|
| `A` | `sonicvpn.ru` | **IP вашего VPS** (шаг 1) | 🟠 **Proxied (вкл)** |
| `A` | `api.sonicvpn.ru` | **IP вашего VPS** | 🟠 **Proxied (вкл)** |

> 🟠 оранжевое облако = «прокси» — значит Cloudflare стоит между посетителем и VPS (заслоняет и ускоряет). Обе записи — оранжевые.
> (Ко второй стране — узлу 2 — позже добавим запись `node2.sonicvpn.ru` с БЕЛОМ облаком — только DNS.)

✅ **Проверка:** через 5–15 минут в терминале:
```
curl -I https://sonicvpn.ru
```
Пока сайт ещё не развёрнут — может быть «connect: refused» или 502. Это **нормально** — домен уже работает, сайт появится в шаге 6. Если `curl` говорит «could not resolve host» — подождите ещё (DNS распространяется до часа).

---

### Шаг 6. Разворачиваем сайт и бэкенд на VPS (20 минут)

**Зачем:** кладём наш готовый сервис (он уже полностью написан в GitHub) на VPS и включаем.

Вы внутри VPS (ssh, шаг 2). Команды — **по одной**, ждёте завершения каждой:

```
git clone -b arena/01a05219-vpnstar https://github.com/samagon90/vpnStar.git /opt/sonicvpn
```
→ скачает весь наш проект в папку `/opt/sonicvpn` (30 сек).

```
cd /opt/sonicvpn/server
npm install
```
→ поставит «модули» (зависимости) нашего сервиса (2–5 мин, летят строки, в конце — без ошибок).

```
cp .env.example .env
nano .env
```
→ создаст файл `.env` (шаблон с комментариями) и откроет его в редакторе nano.

**Теперь в nano находим строчки и заполняем** (правая стрелка — в конец строки, стираем старое, вписываем новое):

| Строчка в .env | Что вписать | Откуда |
|---|---|---|
| `BASE_URL=` | `https://sonicvpn.ru` | ваш домен |
| `SECRET_KEY=` | любое длинное случайное — в терминале выполните `openssl rand -hex 32` (выйдите из nano: `Ctrl+X` без сохранения, если не сохранили) и вставьте результат | сгенерировано |
| `PAYMENT_MODE=` | пока `mock` (режим теста) | — |
| `TG_BOT_TOKEN=` | токен бота | шаг 7 (если бот ещё не создан — оставьте пустым, впишете позже) |
| `TG_CHANNEL_URL=` | `https://t.me/sonicvpn_channel` | имя канала из шага 7 |
| `AI_API_BASE=` | `https://api.groq.com/openai/v1` | шаг 8 |
| `AI_API_KEY=` | `gsk_...` | шаг 8 |
| `AI_MODEL=` | `llama-3.3-70b-versatile` | шаг 8 |
| `XUI_BASE=` | адрес панели 3x-ui, например `http://127.0.0.1:2053` | шаг 3 (панель на этом же VPS → `127.0.0.1` вместо IP) |
| `XUI_USER=` | логин панели 3x-ui | шаг 3 |
| `XUI_PASSWORD=` | пароль панели 3x-ui | шаг 3 |
| `XUI_HOST=` | `sonicvpn.ru` | домен |
| `XUI_PORT=` | `443` | шаг 4 |
| `XUI_SNI=` | `www.microsoft.com` | шаг 4 |
| `XUI_PUB_KEY=` | **Public Key** | шаг 4.1 |
| `ADMIN_TOKEN=` | `openssl rand -hex 16` (как выше) | сгенерировано |
| `YOOKASSA_*` | пока пусто | шаг 9 (после ЮKassa) |

Сохранить: `Ctrl+O` → Enter → `Ctrl+X`.

**Запускаем и делаем «бессмертным»:**
```
pm2 start src/index.js --name sonicvpn
pm2 save
pm2 startup
```
(последняя команда напечатает `sudo ...`-команду — выполните её, если попросит).
→ pm2 запустит сервис и поднимет его автоматически после любой перезагрузки VPS.

```
pm2 logs sonicvpn
```
→ в логах должна быть строка `Sonic VPN: http://0.0.0.0:8080 ...`. `Ctrl+C` — закрыть логи (сервис продолжит работать).

**Проверяем через домен** (SSL-сертификат Caddy выдаёт сам за 5–15 минут):
```
curl -I https://sonicvpn.ru/api/health
```
Через 5–15 минут появится: `HTTP/2 200` и `{"ok":true,...}`.

✅ **Проверка:** откройте в браузере **https://sonicvpn.ru** — видите лендинг Sonic VPN с логотипом. Зарегистрируйте тестового пользователя (вход → «Регистрация» → логин+пароль) → в кабинете QR-код. **Сайт жив.** 🎉

> Пока `PAYMENT_MODE=mock` — в чекауте будет кнопка «Я оплатил» (так тестируем). В бою включим ЮKassa в шаге 9.

---

### Шаг 7. Создаём Telegram-бота и канал (30 минут, 0 ₽)

**7.1. Бот:**
1. Telegram → поиск **@BotFather** (с галочкой) → `/start`.
2. Впишите: `/newbot`
   - Имя бота: `Sonic VPN`
   - Username (техническое имя, в конце обязательно `_bot`): `sonicvpn_bot`
3. BotFather пришлёт **токен** — длинную строку вида `123456789:AAH...`. **Скопируйте** → впишите в `.env` (`TG_BOT_TOKEN=...`) → `pm2 restart sonicvpn`.
4. В BotFather: `/mybots` → Sonic VPN → **Bot Settings → API URL** → впишите `https://sonicvpn.ru` → Save.
   (Зачем: из-за этого на сайте появится рабочая кнопка «Войти через Telegram».)

**7.2. Канал (новости сервиса):**
1. Telegram → «Новое сообщение» → **New Channel** → имя `Sonic VPN` → **Public** → username `sonicvpn_channel` → Готово.
2. (Опубликовать что-то можно позже; ссылка уже встроена в сайт и бота.)

✅ **Проверка:** напишите боту `@sonicvpn_bot` `/start` → он приветствует, даёт меню («Моя подписка», «Покупка», «Поддержка»…). Зарегистрируйте этот Telegram аккаунтом с сайта (кабинет → «Войти через Telegram») — аккаунты синхронизируются.

---

### Шаг 8. Подключаем бесплатную нейросеть поддержки (5 минут, 0 ₽)

**Зачем:** чтобы «❓ Поддержка (нейросеть)» в боте отвечала по-человечески. Бесплатно, карта не нужна.

1. **console.groq.com** → **Sign Up** (можно через Google/GitHub) → примите условия → **API Keys → Create API Key** → скопируйте ключ `gsk_...`.
2. В `.env` (вы уже вписали `AI_API_BASE` и `AI_MODEL` в шаге 6) впишите `AI_API_KEY=gsk_...`.
3. `pm2 restart sonicvpn`.
4. Тест: в боте → «❓ Поддержка (нейросеть)» → напишите: `Сколько стоит 3 месяца?` → бот ответит: 499 ₽ и т.д. Напишите: `До когда действует моя подписка?` → ответит **вашей** реальной датой (берёт из базы).

> Лимит: ~1000 вопросов/день бесплатно — на старте за глаза. Если Groq когда-нибудь станет недоступен — бот сам переключится на встроенный FAQ (клиенты не останутся без ответа). Запасные ключи (OpenRouter, Gemini) — в `deploy/README.md`, шаг 4.1.

---

### Шаг 9. Включаем настоящие платежи — ЮKassa (когда одобрят, 30 минут)

1. На почте — письмо от ЮKassa (одобрено). Заходите в **личный кабинет yookassa.ru**.
2. **Shop ID** — вкладка «Мой магазин» (число).
3. **Secret Key** — вкладка «API-ключи» → «Создать» (секретный, скопируйте сразу — повторно не покажут).
4. **Webhook** — вкладка «Webhooks» → «Добавить»:
   - URL: `https://sonicvpn.ru/api/webhooks/yookassa`
   - События: `payment.succeeded` и `payment.canceled`
   - (Токен проверки = ваш Secret Key — программа уже умеет проверять.)
5. В `.env`:
   ```
   PAYMENT_MODE=yookassa
   YOOKASSA_SHOP_ID=<Shop ID>
   YOOKASSA_SECRET_KEY=<Secret Key>
   YOOKASSA_IS_SANDBOX=false
   ```
6. `pm2 restart sonicvpn`.
7. **Реальный тест:** зарегистрируйтесь (новый аккаунт) → «Купить» → 1 месяц 199 ₽ → **СБП QR** → оплатите **честные 199 ₽** (или сначала 1 ₽ в песочнице, если включите `YOOKASSA_IS_SANDBOX=true` и протестируете, потом переключите на `false`).
8. После оплаты: подписка активна, в истории — «оплачен», на вашу карту (через ЮKassa) придут деньги с чеком самозанятого. Комиссия ЮKassa за СБП — 0,4%.

✅ **Проверка:** деньги в банке/приложении банка (через пару минут), подписка в кабинете активна, чек прилетел на email/в «Мой налог».

---

### Шаг 10. Финальный тест всего (1 час, по чек-листу)

Проходим от начала до конца как клиент:

- [ ] `https://sonicvpn.ru` открывается на телефоне **на мобильном интернете РФ** (без какого-либо VPN)
- [ ] Регистрация: логин+пароль, **без** почты и номера → сразу 7 дней пробника
- [ ] Кабинет: QR «Основного» → импортируем в v2rayNG → YouTube работает
- [ ] «Добавить устройство» (2-е, например `iPhone`) → свой QR → работает с другого телефона
- [ 3-е устройство: кнопка «Купить +1 — 99 ₽» → оплачиваем СБП → появляется
- [ ] Блокировка: «Заблокировать» 2-е устройство → оно отваливается → «Включить» → снова работает
- [ ] Оплата 199 ₽ по СБП → подписка продлилась → в истории «оплачен»
- [ ] Рефералка: второй человек регистрируется по вашей ссылке (`https://sonicvpn.ru/auth.html?ref=ВАШ_КОД`) → платит → вам на баланс 20% (39,80 ₽ с 199 ₽)
- [ ] Бот: `/start` → «📶 Моя подписка» (QR) → «📱 Мои устройства» → `/devices` → `/block 1` → `/unblock 1`
- [ ] Поддержка в боте: «Как заблокировать устройство?» → внятный ответ

Если всё галочки — **сервис запущен**. 🚀

---

## Часть 3. Частые проблемы (и что делать)

| Симптом | Причина → решение |
|---|---|
| `ssh: Connection refused` | VPS ещё поднимается (первый раз до 10 мин) или упал → перезагрузите в кабинете провайдера |
| `Permission denied (password)` | Ошиблись паролем root → в кабинете провайдера: «Сбросить пароль» → новый пароль |
| Порт 3x-ui не открывается в браузере | Firewall провайдера закрывает нестандартные порты → в кабинете VPS откройте нужный порт (например 2053/tcp) в firewall-правилках |
| `curl: could not resolve host sonicvpn.ru` | DNS ещё «разлетается» (до 1–2 ч) → подождать; проверить, что NS у reg.ru именно Cloudflare |
| Сайт не открывается, `502/503` | Сервис ещё не запущен или падал → `pm2 status`, `pm2 logs sonicvpn` — читать последнюю ошибку |
| SSL: «not secure» сразу после деплоя | Caddy ещё выпускает сертификат (5–15 мин) → подождать; потом — зелёный замок |
| YouTube не грузится через VPN | Проверьте: порт 443 открыт, Public Key в .env совпадает с панелью, SNI = `www.microsoft.com` |
| Бот молчит | `TG_BOT_TOKEN` пустой/с ошибкой → проверить `.env`, `pm2 restart sonicvpn`; в BotFather → API URL должен быть `https://sonicvpn.ru` |
| Кнопка «Войти через Telegram» не работает | Не задан API URL у бота (шаг 7.1.4) или `TG_BOT_TOKEN` пустой |
| Оплата не активирует подписку | Webhook не сохранён/URL с опечаткой → проверить в ЮKassa; `pm2 logs` — строка `[webhook] payment ... paid` |
| `429` от Groq в логах | Лимит бесплатных запросов исчерпан → бот сам ответил по FAQ; спросит клиент — перезадать через минуту; запасной провайдер — OpenRouter |

## Часть 4. Рутина (1 раз в неделю, 5 минут)

```
# бэкап базы и ключей:
cd /opt/sonicvpn
tar czf /root/sonicvpn-backup.tgz data server/.env
# перенести /root/sonicvpn-backup.tgz себе (scp с VPS на компьютер)
# проверить, что сервис жив:
pm2 status
```

- Автоплатёж VPS — уже включён (шаг 1), просто следите за балансом карты.
- Заблокировали IP узла (редко): новый IP у провайдера → обновить A-записи в Cloudflare + `XUI_HOST` (если IP, а не домен — у нас домен, так что чаще всего Cloudflare-записей достаточно) → новым клиентам новый профиль в кабинете.
- Сменить логотип: заменить файл `public/img/logo.png` (в репо и/или на VPS: `cd /opt/sonicvpn/public/img`) — поменяется везде.
- Поменяли цены/текст поддержки: `server/src/config.js` (цены) и `server/src/ai.js` (ответы бота) → `pm2 restart sonicvpn`.

## Часть 5. К чему это приведёт (цифры)

| Расход (из РФ) | Сумма |
|---|---|
| VPS Германия | ~400–600 ₽/мес |
| Домены | ~60 ₽/мес |
| Cloudflare / бот / нейросеть / софт | 0 ₽ |
| **Итого** | **~500–700 ₽/мес** |

| Доход | 199 / 499 / 1 499 ₽ за тарифы + 99 ₽ за устройство. Минус 0,4% (СБП) и 4% (налог). **4–6 клиентов закрывают все расходы.** |
