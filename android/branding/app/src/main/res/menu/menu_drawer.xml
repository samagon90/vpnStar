<?xml version="1.0" encoding="utf-8"?>
<!-- Sonic VPN: урезанное меню — только то, что нужно обычному пользователю.
     Убрано: подписки, per-app прокси, маршрутизация, ассеты, реклама v2rayNG,
     logcat, проверка обновлений (вела на чужой релиз!), бэкап. -->
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:id="@+id/group_main">
        <item
            android:id="@+id/settings"
            android:icon="@drawable/ic_settings_24dp"
            android:title="@string/title_settings" />
        <item
            android:id="@+id/debug"
            android:icon="@drawable/ic_feedback_24dp"
            android:title="@string/title_debug" />
        <item
            android:id="@+id/about"
            android:icon="@drawable/ic_about_24dp"
            android:title="@string/title_about" />
        <!-- place holder for version text at the bottom -->
        <item
            android:id="@+id/placeholder"
            android:enabled="false"
            android:title="" />
    </group>
</menu>
